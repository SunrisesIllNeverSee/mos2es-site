.netlify/
.DS_Store
**/.DS_Store

# 11ty build output
_site/

# Node dependencies
node_modules/

# Claude Code internal files (keep settings.json for shared hook config)
.claude/*
!.claude/settings.json

# Session tracker state (per-session, per-machine — don't commit)
Devins_Plans/state/.role-*
Devins_Plans/state/.session-current

# Repository Standard runtime state
.coord/micro/state/.session-current
.coord/micro/state/.role-*
.coord/micro/state/ACTIVITY.log
.coord/micro/state/snapshots/
.coord/micro/scratchpad-archive/
.repo/.cache/
