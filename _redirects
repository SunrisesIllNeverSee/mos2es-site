# Cloudflare Pages handles pretty URLs natively (serves /page.html at /page).
# The previous Netlify-style _redirects rules (/path -> /path.html 200) caused
# redirect loops on Cloudflare Pages because they conflicted with the built-in
# pretty URLs feature. This file is intentionally empty.
